{% unless coll %}
  {% comment %}
  if the current collection is filtered with a tag then the collection object
  will not have all the tags for the entire collection, just the tags left
  by the filter. To get around this we find the current collection in the
  global collections object and then use that for the filter menu. The
  collection object from the global collections does contain all the tags
  for the collection, regardless of the current tag filter.
  {% endcomment %}
  {% if collection %}
    {% for current_coll in collections %}
      {% if collection.handle == current_coll.handle %}
        {% assign coll = current_coll %}
      {% endif %}
    {% endfor %}
  {% endif %}
{% endunless %}
{% if coll.handle == 'shop' %}
  {% assign sub-menu-categories = 'New Arrivals, Evening Gowns, Cocktail Dresses, On Sale' | split: ', ' %}
  {% comment %}
  {% elsif coll.handle == 'bridal' %}
  {% assign sub-menu-categories = 'Bridal 2013, Bridal 2014, Bridal 2015, Bridal 2016, Bridal 2017' | split: ', ' %}
  {% endcomment %}
{% elsif coll.handle == 'collections' %}
  {% assign sub-menu-categories = 'Resort 2016, Spring 2015, Fall 2015, Pre-Fall 2015' | split: ', ' %}
{% else %}
  {% assign sub-menu-categories = false %}
{% endif %}
